#include <vector>
#include <string>
using namespace std;

struct Faculty {
    string name;
    vector<string> title;
    int fundingRequired;	//increment or decrement by event 
    int reputation;		//stat for now 
    int student_life;
};
